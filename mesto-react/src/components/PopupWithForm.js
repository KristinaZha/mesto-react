function PopupWithForm(props) {
    return (
        <div className={`popup popup-${props.name} ${
            props.isOpen & "popup popup_opened" : "popup"
        }`}
        >
            <div className={`popup`}
    )
}